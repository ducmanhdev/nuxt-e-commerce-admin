export default defineAppConfig({
  ui: {
    colors: {
      primary: 'rebeccaPurple',
    },
    input: {
      slots: {
        root: 'w-full',
      },
    },
    selectMenu: {
      slots: {
        base: 'w-full',
      },
    },
  },
})
